sudo: false
language: ruby

rvm:
  - 2.1.10
  - 2.2.8
  - 2.3.5
  - 2.4.2
  - ruby-head
  - jruby-9.1.9.0    
  - jruby-head 
matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-head

before_install: gem install bundler -v 1.16.1

# whitelist
branches:  
  only:
    - master
